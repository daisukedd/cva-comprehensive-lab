import { button } from './src/components/button';
import { badge } from './src/components/badge';

console.log(button({ color: 'red', size: 'lg' }));
console.log(badge({ color: 'blue', size: 'md' }));
console.log(badge({ color: 'green', size: 'lg' }));